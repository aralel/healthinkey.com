# Changelog

## Unreleased
- Added initial project plan (`PLAN.md`).
- Added GitHub Pages-ready landing page (`index.html`, `assets/styles.css`, `assets/app.js`).
- Added `README.md` with local preview and GitHub Pages setup.
- Wired favicon and app icons from `assets/icons` into `index.html`.
- Updated `browserconfig.xml` and `manifest.json` to reference `assets/icons` paths.
- Improved small-screen responsiveness (spacing, typography, hero card, CTA).
- Switched header and footer brand to use `assets/healthinkey_logo_v1.png`.
- Redesigned header layout (container-wrapped nav, always-visible section links).
- Redesigned footer into brand, links, and meta/back-to-top areas.
- Adjusted footer to use the main logo size (no `brand-logo-small`) with tagline directly below the logo.
